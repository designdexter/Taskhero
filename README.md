# Taskhero
TaskHero — a React Native (Expo) app that helps parents assign tasks to kids and reward them with points. Built with a design-system-first approach: Figma → Storybook → Expo. Uses Supabase for auth, database, storage, and realtime sync.
